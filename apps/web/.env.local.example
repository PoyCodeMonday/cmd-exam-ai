# Default browser-side API base. Leave unset to use the same-origin '/api' proxy.
# Only set this if you want the browser to talk directly to an external API URL.
# NEXT_PUBLIC_API_URL=

# Where the Next.js /api proxy route forwards to (server-side only).
INTERNAL_API_URL=http://localhost:8080
